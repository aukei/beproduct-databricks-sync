# bootstrapping

## General info

### Background
- Prepare pipeline that sync data between BeProduct and DTC
- Both BeProduct and DTC are SaaS with RESTful API - sparsely documented
- BeProduct has a python SDK further abstracting part of its API

### SDK & API specs
- BeProduct SDK: https://python.beproduct.com/ (doc) https://github.com/BeProduct/BeProduct.Python.SDK (implementation)
- BeBroduct API: https://developers.beproduct.com/swagger/v1/swagger.json  (Swagger JSON)
- BeProduct environmnts to consider: only 1 environment, data split by Folder
- DTC API & endpoints: ./dtc/DTC-api-2026-05-08.json (postman API project dump), ./dtc/DTC-api-2025-05.pdf (description and example)
- DTC environments to consider: UAT, PRD

## Basic assumptions
- BeProduct, DTC are JSON stores
- BeProduct roughly follows a Parent - Child data model. "STYLE" is the header, link with "Size", "Colorways", "BOM" that store details
- DTC is a Excel-like genric data entry tool. In this project relavent data are store denormalized in a flat wide worksheet.

## General directions
- DTC prefer patch API (=update)to push API (=overwrite)
    - "Document" defines Json schema
    - "Request" instantiate "Documents", hold metadata e.g. RequestName, Owner etc
    - "Sheet" hold actual data of a "Request". As of today, All Requests have 1-to-1 relationship to a Sheet.
    - "View" is subset of fields of a "Document" applicable to any "Request"/"Sheet" of that same type.
    - Document identified by document_id
    - Data identified by (request_id + sheet_id)
    - View identifed by view_id
    
- BeProduct data domains: Styles, Materials, Colors, Images, Blocks, Directory, MasterData
- If Databricks is involved, tables to be put under Unity Catalog lft.beproduct
    - Access ADB with 'databricks' CLI

- BeProduct has full timestamp with timezone (UTC). DTC output timestamp as UTC (on retrieve). DTC expects input timestamp in user profile timezone, treat as +0800 HKT in current setup.

- set http proxy to http://100.64.0.7:8888 before running DTC API tests.


# Phase 1 $25

## Objectives
- Sync selected fields from BeProduct "Style" to DTC "WIP" Requests
- Customer to focus on = KTB
- On DTC, denormalized to STYLE x COLOR x BOM
- The integration workflow to be scheduled and run entirely in Databricks as job
- Data that should be synced
    - Read from BeProduct: [Product Status, Image, LF Style number, Description, Product Category, Product Sub Category, Division, Brand, Color, Garment Finish, Tech Pack Stage, Group, Fabric Placement, Fabric Article]
    - Upsert to corresponding sheet/row in DTC: [Product Status, Style Image, LF Style#, Style Description, Class, Sub Class, Division, Brand, Color / Wash, Garment Finish, Tech Pack Stage,Fabric Group, Placement, Mill Fabric Article #]
    
    
## known data structure and access patterns
- BeProduct: Reference streamlit application implementation in ./app/ui/main.py
    - BeProduct to Databricks: project artifacts in ./databricks/
    - A style is uniquely identified by Customer + Brand + LFStyle# + Season
        - Customer = Folder Name
        - Brand = $."headerData"."fields"[name ="brand"]
        - LFStyle# = $."headerData"."fields"[name = "LF Style Number"]
        - Season = $."headerData"."fields"[name = "SEASON"]
    - 1 Style link with 1 Colorway
        - Data are in $."headerData"."fields"
        - A Colorway can contain more than 1 color name
            - Array $."colorways"
            - Interested in $."colorways"[]."colorName"
        - i.e. 1 style : n color = n DTC rows
    - 1 Style link with 1 BOM
        - Each BOM is a datagrid of Material (i.e. Fabric) + Color
        - There is currently NO direct API to retrieve BOM data
        - CURRENT PHASE: hard code each style to have **1 BOM line** per (style × color):
          (Fabric Group, Placement) = ("MAIN MATERIAL CONTENT", value of $.headerData[id="main_material_content"].value)
          - i.e. 1 style : n color : 1 BOM = n DTC rows
        - NEXT PHASE: introduce a `style × bom` table; the transform will then explode
          to (style × color × bom) rows instead of the single hardcoded BOM line.
        

- DTC: Reference ./EXPLORATION_SUMMARY.md, ./databricks/dtc/README.md
    - DTC to Databrricks: project artifacts in ./databricks/dtc/    
    - This phrase primarily focus on Requests of the type "WIP" document using the "WIP_ITS_USE" view (i.e. complete data)
        - Workspace = "<Customer>", i.e. "KTB"
        - Document = "<Customer> WIP", i.e. "KTB WIP" in this instance 
    - DTC Requests are named "<Customer> <SeasonCode> <Brand>"
        - SeasonCode is always 4 character string, e.g. SS26, SS27, FW28
        - Brand is everything after SeasonCode including spaces
        - SeasonCode = SSYY = 2-characters season + 2-digit year.
            - use databricks lft.beproduct.dtc_seacode_mapping (customer, BeProduct Season, DTCcode)            
            - DTC season = SSYY; BeProduct "Year" = 20YY
            - all other season code log errors
    - Use DTC "patch" API for existing (style + color + material)   >> keep "sheetId" / "rowIndex". "rowID" can be used in lieu of "rowIndex" 
        - put in all fields of that row, even if the value is unchanged
        - ref page 18 of pdf: PATCH /v1/sheets/{sheetId}/views/{viewId}

    - Use DTC "patch" API also for new (style + color + material)  >> assign a new rowIndex (max(rowIndex) of current sheet + 1)
        - put in all fields of that row that have value
        
    
    - Retrieve DTC data: get_requests(workspace + document), returns ALL matching (request ID + sheet ID)
    - Patch data: patch(sheet id + view ID [+ rowIndex] + delta data), preferably only affected row (i.e. with rowIndex or rowID)

- Mapping process 
    - match [customer + brand + style# + season + color + material]    
    - new Seasoncode / Brand: need new DTC Sheet
        - Create sheet () return request id + sheet id
        - Then use Patch api to INSERT (dense_rank() over partition by brand/customer/seasonId)
        - For each row do ImageInsert flow below
        - "create sheet" API on p.15 - POST /v1/sheets. This should return both requestId and sheetId on success.
    - new rows on existing sheet:
        - patch api require assign rowIndex (max(DTC sheet)+1)
        - For each of these rows, do ImageInsert flow
    - Update existing row:
        - BeProduct_Style_modifiedAt > DTC_row_Updated_at *beware of timezone*
        - keep request ID, sheet ID, row ID, row Index
        - Non image fields: assign those interested to DTC row
        - Image:  separate from and after row CRUD, for each row updated
            - if DTC.Style_Image is blank, do ImageInsert flow
            - else next row - assume Front image will not change
            "headerData"."frontImage" only. 
             
    - In case BeProduct rows < DTC rows, *"mark (extra DTC rows) Product Status = Drop"*, do not "DELETE rowid"
    
    - **separate add / update image into dedicated notebook** so we can tackle it later.

    - ImageInsert flow
        - If BP "headerData"."frontImage"."origin" is a proper url, use DTC API /v1/sheets/{sheetId}/views/{viewId}/images?rowindex={number}&columnname={text} to push binary into DTC
        - Take "origin" from BeProduct CDN. Treat as binary data.
        - "add sheet image" API on p.16 - POST /v1/sheets/{sheetId}/views/{viewId}/images?rowindex={number}&columnname={text} using multipart/form-data.
        

        
## Tentative flow
- Batch based
1. download BeProduct: Masters + Style + ColorWay
2. download DTC: all WIP Document of <workspace>. Decode Request name & Keep track of "sheetID - <brand> <session>" mapping
3. Join (1) to form full view of current BeProduct Image
4. Compare (2) to (1):
    4.1. For those "1" not matching any "2" > mark for PUSH
        4.1.1. Determine if the DTC Request exists. If not, LOG ERROR
        4.1.2. If exist, push the rows to that Request
    4.2: For those "1" that has matching row(S) in "2", mark for Patch
        4.2.1. For each ROW, call a patch on that Request_id + row_id

## Revised tentative flow Base on latest DTC admin guidelines: 
- For phase 1, Customer = KTB (user param), workspace = ${custome}> = KTB (no change), Document = "${customer} WIP" = "KTB WIP" (no change), view = "WIP_ITS_USE" (no change, user param)
- All "Requests" in scope are named "${customer} ${DTC seasoncode} ${brands}" e.g. "KTB FW26 Wrangler". There are many requests of the same Document of other names convention in the workspace, ignore them.
- Tentative flow:
    1. Upload all Requests to Databricks, 1 table for each ${workspace} + ${customer}, so the Databricks DTC table contains additional [Request ref, Seasoncode, Brands] cols.
        1a. Perhaps an extra control table that stores [request ID, view id, customer, seasoncode, brands, last_extracted, msgs] for sync control
        1b. A Request / View can be empty i.e. no rows
    2. Ensure BeProduct Style is synced - at least last last_extracted should be of same day.
    3. Data massage - reference "docs/beproduct_style_interested_fields.txt"
        3a. BeProduct -> DTC: perform upsert on DTC table using BeProduct transformed table on (LF Style#, Seasoncode, Brand), update all indicated non-key fields EXCEPT "Style Image", or write new rows with key fields.
        3b. RowIndex: it is effectively the row_number() over (partition by Seasoncode, Brands) but DTC allow sparse rowIndex. Therefore in case of update, keep original RowIndex. In case of insert, assign coalesce(max(RowIndex) over (partition by Seasoncode, Brands) + 1,1)
        3c. log exceptions to sync log table.
    4. Push back massaged DTC data
        4a. Require delta push. Updated / new rows should have a modified timestamp > last_extracted tracking
        4b. for each update row, use PATCH api to push all non key fields. 
        4c. for each new row, use PATCH api to push all field all fields, with calculated RowIndex value.
        4d. log detail exceptions to sync log table.
    5. No push back to BeProduct in this phase.


# Phase 1 Checking & Clarifications 

- valid IN-SCOPE 'KTB ...' UAT request ID (sacrificial requestId) = 6a26581854e92e7acd8fa71b
- Within a single in-scope request (e.g. 'KTB FW26 Wrangler'), can the per-row 'Brand' column contain multiple brands, or is it exactly one brand per request? (The KON sample mixes Modern/Wrangler/Lee, but that's out of scope.) This determines the (LF Style#, SeasonCode, Brand) match semantics.
    - The project team assure me that this col Brand will be read only from user and is only populated by this suite of scripts. therefore only 1 brand per request, and it must agree with the request name. KON are developer test case without catering data consistency.
- For validating the push (PATCH writes mutate live DTC data), how should I proceed in this phase?
    - *Revised* all would-be requests to be created and share right assigned (see ENHANCEMENT)
    - *original scope* should have been pre-created by project team, and they ought to be empty. if they are not empty, treat as sacrificial (honor the upsert logic anyway). if request does not exist then mark error.

## Enhancement 2026/06/18
Enhancement to DTC sheet creation: from DTC developer, system assign full right to sheet creator at create time. for data to be visible to team, need to "Share" it to others (p.5-6 of pdf spec = POST POST /v1/requests/{requestId}/shares/{userEmail} , POST/v1/requests/{requestId}/shares/usergroups/{userGroupName})
1. Share all views to "AI Agent WIP <aiagentwip@lifung.com>"
-- superseded 2026/08/20 2. Share "Full Version" view to "Fabric Group"
3. Patch previous requests created during test.


# Phase 2  $148
Phase 2 DTC -> BeProduct seems trivial at this point, maybe add together. In this phase:
1. cater rows that have key fields updated in beproduct causing DTC rows fall into different request.
2. these DTC values updated to BeProduct corresponding style and push back to beproduct: "Legacy Code", "Lot#", "Main Vendor (Sampling)", "Main Factory (Sampling)", "Main Factory Customer ID"
3. High level flow:   BeProduct sync to Databricks + transform, DTC sync to Databricks, Use Beproduct transform to update DTC, use DTC to update BeProduct transformed (*1), Push back DTC, Push back BeProduct.
4. (*1) BeProduct Transformed is denormalized by color & (To be) material. Since at least "Lot#" is on colorway array, so may need a reverse transform to BeProduct raw style before push back. Evaluate whether a direct from transformed table pushback or this make more sense.

## Clarifications
The interested-fields doc lists 'Main Factory Customer ID' with a BLANK BeProduct source — there is no mapped BeProduct field. Where should DTC's value be written on pushback?
Skip it for now
Point 1: when a BeProduct key field (LF Style#, brand, or season) changes, the row's DTC request changes (e.g. brand Wrangler→Lee moves it to a different request). The new request gets an INSERT. What about the now-stale row left in the OLD request?
mark stale DTC row "Product Status" to "(removed)" - this is not a valid BeProduct value, so act as a signal to DTC user .

## Tidy up 2026/06/17
1. rewrite  @dtc/PHASE1_WORKFLOW.md  similar to PHASE2_WORKFLOW documenting BeProduct to DTC dataflow. I have backup its original content to @implement_prompts.txt  for my own future reference. 
2. what is the best way to refactory docs / agent notes so that subsequent changes to field mapping via another kilo agent can pick up all the previous discovery?
3. commit and update databricks notebook. 
4. give a summary of steps to run the 2 workflows.


# Phase 3 Image $44
Tackle image. Since image API is an operation on existing row, it cannot be handled at record insert time. Therefore tentatively:
1. as an independent step after p1p7_beproduct_to_dtc_push, reload all in-scope request/sheet rows from dtc ie. refresh dtc_wip_ktb, noting "Style Image" of each row is populated or not.
2. if  the row "Style image" attribute is not populated, and corresponding BeProduct row has valid "front_image_url", then use p.16 "POST /v1/sheets/{sheetId}/views/{viewId}/images?rowindex={number}&columnname={text}" API to add image to the row.
2.1 {columnname} should be "Style Image"
2.2 Body is retrieved from front_image_url (e.g. curl), then put into request body in binary format as multipart/form-data


# Checkpoint clean up 1 on docs $12
- move PHASE*WORKFLOW to docs, update if necessary
- add PHASE 3 WORKFLOW
- ensure AGENTS.md, README.md correctly describe repo
- ensure QUICK_START.md describe "setup, how to use, which notebook to run"
- revise docs/ARCHITECTURE.md to cover components & data flow & data model on ADB - merge BEPRODUCT_TO_DTC_GUIDE.md, dtc/README.md, dtc/DATA_MODEL.md
- i combined docs/requirements.md into /implement_prompts.txt
- refactor docs/DTC_GUIDE.md BEPRODUCT_GUIDE.md so each cover that component's relevant API/SDK that jobs used, as well as relevant data model on ADB. 
- suggest other project structure cleanup that is worthy. DO NOT modify py/notebook scripts at this point.

# Checkpoint clean up 2 on scripts
- upload_to_databricks.py - bin it
- beproduct_style_push - move to standalone/ with accompany notes
- sure refresh kilo skills
- kilo plan - superseded by docs. bin it
- implement_prompts - keep at /, but create a docs/prompts/opus_revision.md about the implementation (exclude task log elements) base on your understand of the prompt plus proj status, and write it in agent friendly way.
    - i intended @docs/prompts/opus_revision.md  be like "how would you opus have written the prompt to this coding agent to create this what we have in this repo", intended for other (human) to learn the way to prompt effectively.
- keep DTC API doc as is.


# Performance tuning $21
DO NOT make change to any code yet. Continue investigate performance of databricks job orchestrate_sync. 
The latest run https://adb-781381861146191.11.azuredatabricks.net/jobs/22324120218492/runs/57360453476718 is after implementation of "parallel DTC read at step 3", "incremental BeProduct Style upsert at step 1" and "Skip rereading no-insert DTC sheet at step 7". Only "skip rereading at step 7" made material differences. First focus on identifying where time was spent in step 1 and step 3. 
My hypothesis is the many 'log message to sync control table' actions being executed as separate spark job that killed performance. And "BeProduct Style list action overall take similar amount of time to response, filter only reduce amount of data return".

There is a docs/performance.md for base line.
databricks-integration SKILL has some instruction on how to get job log from databricks written by another agent. Make sure or update that if relevant.


# Phase 4 sample information $33
- Reference to BeProduct SDK 'https://python.beproduct.com/075-apps/', each Style can have a list of associated applications. 
- Take LFStyle# 'HOODED-K263' & 'LFBP-WM1MO-MALE WAXED JKT-002' as examples, study what applications are there, taking note of application IDs which is the same for a particular app across all Styles according to doc. Verify this claim.
- In particular is there apps resemble 'Proto Sample', 'PreLine sample'. If so, what content data do BeProduct returns in 'app_get' call? 
- User want to see "submitStatus" and "submitStatusDate" of  all sample apps on DTC. Since these apps have a separate 'modifiedAt', likely
    - do full style retrieval every time
    - enrich with apps data
  Ponder data to see would there be better ways: Do Style contains any metadata that hint app's Modified?
- in this exploration phase, just do a full style x sample trial run locally to ensure we deduce app behavior correctly, and to time it. AFAIK the project team only setup Sample data in a few Style during implementation. Good to know which Style is a good candidate for later coding verification purpose too.
    - note: The tricky part is deciding which submit status to extract when an app has multiple submits and sizes. The cleanest approach is to take the latest submit (last in the submits array or by submitStatusDate) and within that, prefer the sample size if it exists, otherwise use the first size. That gives us a single submitStatus and submitStatusDate per app to store in the corresponding column.
    - each submit has:       → submit=1ST Submit size=S status=Requested statusDate=2026-05-14
- save the whole sample submit list to style.

# Phase 5 Master Data utility
verify @beproduct/p5utl_beproduct_master_data_sync.py  does it still work against current beproduct data structure?  include "directory" as part of master data, ref  SDK at https://python.beproduct.com/080-directory/
- note SDK does not support update / insert to Directory, however RESTful api at https://developers.beproduct.com/swagger/v1/swagger.json expose /api/{company}/Directory/Add and /api/{company}/Directory/Update.
- derive push back logic of Directory as well as other dropdown values.
- this is  manually triggered by admin to check and bulk update beproduct master data & directory, thus not part of DAG schedule job.
- it is known that the directory list API throughput is 2rec/sec. So push and pull ops should be separated.
- Directory "update" is actually massaging another table to upsert record to this directory master. Add a cell containing a template merge SQL that cater the modifiedAt logic, and admin can supply the actual merge statement later.

## master data sync between (XTS) DTC <> BeProduct
Before any Style/Material/Costing sync steps, some master data should be upsert into BeProduct from DTC.
This is actually phase 0 in logical sense.
- DTC workspace "KTB" Document "XTS Master"
    1. Request ref "XTS Supplier Master" for partner type "SUPPLIER" 
    2. Request ref "XTS Factory Master" for partner type "FACTORY"
    /*3. Request ref "XTS Mill Master" for partner type "MILL"*/
- DTC request structure mirror and should upsert into lft.beproduct.beproduct_directory table, primary key on "name"
    - Despite there is 'id' and 'directory_id' fields, BeProduct treat "name" + "type" as key.
    - [Supplier|Mill|Factory] Name -> 'name'
    - [Supplier|Mill|Factory] Code -> 'directory_id'
    - 'Type' by ref name above
    - all the rest: address, country, state, zip, city, phone, fax, website, notes are optional 
    - Production Country -> 'country'
- Verify p5utl master data sync can upload any changed / new record to BeProduct master.
- the daily DAG now require to push any new or updated DTC supplier / facory master to BeProduct directory


# Phase 6 Structural clean up $12
- BeProduct Style contains 2 new fields LABEL = "BP STYLE NUMBER", "Brand", no "Legacy code" 
- "BP STYLE NUMBER"  replace "LF_STYLE#" in the composite key that identify a Style.
    - BP "BP Style Number" -> DTC "BP Style#"
- "Brand" not "Brands" in the composite key. Its field_id is 'brand_hk'. Ignore field_id 'brand'.
- i.e. composite key is now [Customer, BP Style#, seasoncode, Brand]
- "LF STYLE NUMBER" is now an optional field on Beproduct Style. Both "LF STYLE#" and "Legacy Code" are now populated from BeProduct to DTC. They are both optional fields (i.e. can be blank). 
    - BP "Customer Style#" -> DTC "Legacy Code"
    - BP "LF Style#" -> DTC "LF Style#"
- add BeProduct style "Gender" -> DTC "Gender"
- DTC WIP document also add field "BP STYLE#" to hold BeProduct's value.
- Validate above with BeProduct Data. If good, update all related sync pipeline and relevant docs sections.


# phase 7 Retrieve Sample information
For each style, of the 6 Sample apps: Proto, PreLine, SMS, Fit, PP, TOP, retrieve the list of "submits" history (if any) and populate corresponding fields on DTC rows.
- Proto Sample -> Proto Sample - Sample Status
- PreLine Sample -> Pre-line Sample - Status
- SMS Sample -> SMS - Sample Status
- Fit Sample -> 1st Fit Sample Approval Status
- PP Sample -> 2nd Fit Sample Approval Status
- TOP Sample -> TOP Sample Approval Status 
Include in the field the complete list of submits info, use submitStatus and submitStatusDate of first size:[name, sizes[0].submitStatus, sizes[0].submitStatusDate] 

## verification 28/8
Some adjustment was made to DTC WIP doc structure, below should be the dest fields for the 6 samples:
- Proto Sample -> Proto Sample - Sample Status
- PreLine Sample -> Pre-line Sample - Status
- SMS Sample -> SMS - Sample Status
- Fit Sample -> 2nd Fit Sample Approval Status
- PP Sample -> PP Sample Submission Approval Status
- TOP Sample -> TOP Sample Approval Status 

Take out the '[[' ']]' in the resulting string.  
- result string reads like "submit1",'Status1","Date1" </br> "submit2","status2","Date2"

# phase 8a Upload DTC Fabric sheets to Databricks, prepare to further upsert to BeProduct Material master.
## superseded by MaterialLib app 2026/09/01
In DTC workspace <customer> i.e. KTB, there are sheets of document "<customer> FABRIC" i.e. "KTB FABRIC". They contains material information to be sync to BeProduct in phase 8B. Here focus On
- Upload all DTC sheets of name "<customer> <seasoncode> <brand>", e.g. "KTB SS28 Blue Bell". Use the view 'WIP_ITS_USE' like the above WIP document. 
- Upload only DTC field "Adoption (Y/N)" = Y rows.
- Trace DTC sheets name uploaded, also similar to WIP document, for potential data push back from BeProduct.
- Key fields of each Fabric (and thus BeProduct Material master compulsory fields): "LF MATERIAL ID", "MATERIAL DESCRIPTION", "MATERIAL CATEGORY", "FABRIC/MATERIAL TYPE", "MILL FABRIC CODE", 'MILL/SUPPLIER NAME"
- known DTC -> BeProduct field mapping (to be confirm)
    "LF MATERIAL ID" -> "LF MATERIAL ID" 
    ? -> "MATERIAL DESCRIPTION"
    ? -> "MATERIAL CATEGORY", 
    ? -> "FABRIC/MATERIAL TYPE", 
    ? -> "MILL FABRIC CODE", 
    ? -> "MILL/SUPPLIER NAME"

# phase 8b upload dtc_fabric to BeProduct
## superseded by MaterialLib app 2026/09/01


# Phase 9a Preparation of Costing Chart
- In addition to WIP / Fabric document requests, Retrieve also Requests of DTC document "<Customer> LinePlan" e.g. "KTB LinePlan", using view "LINEPLAN_ITS_USE" (preferred) or "Full" if former not exists.
- Once WIP and LinePlan are in Databricks, they are joined with WIP "Lineplan Ref #" to LinePlan "Lineplan Ref #" to from a "Costing_Chart":
    - transpose [(Main Vendor, Main Factory), (Vendor 1, Factory 1), (Vendor 2, Factory 2), (Vendor 3, Factory 3)] into 4 rows [Vendor, Factory]
    - Style - Color level
    - key = [Customer, BP_Style_no, LF_style_no, Legacy Code, SeasonCode, Brand, Color_name, Lineplan Ref#, Vendor, Factory]
    - Interested fields: 
BP Style#              - Master Chart
LF Style#              - Master Chart
Style Description      - Master Chart
Season                 - Master Chart
Brand                  - Master Chart
Fabric Content         - Master Chart "Content" column
Gender                 - Master Chart (not yet available in source currently)
Class                  - Master Chart
Sub Class              - Master Chart
Supplier Type          - Generated from Master Chart data
Supplier               - Main Vendor (Sampling) / Vendor 1 / Vendor 2 / Vendor 3
Factory                - Main Factory (Sampling) / Factory 1 / Factory 2 / Factory 3
Production Country     - Master Chart
Order Quantity         - Line Plan "PROJECTED VOLUME (season)" column
Target LDP             - Line Plan "Target SAP w/ Tariff Impact" column
HTS Code               - Master Chart "[Main Factory|Factory 1-3 -] HTS code" or if blank, Retrieve from NT Orbit Duty Tools
Duty Rate (US)         - Master Chart "[Main Factory|Factory 1-3 -] - Duty Rate (US)" or if blank, Retrieve from NT Orbit Duty Tools
Duty Rate (CA)         - Master Chart "[Main Factory|Factory 1-3 -] - Duty Rate (CA)" or if blank, Retrieve from NT Orbit Duty Tools
Duty Rate (MX)         - Master Chart "[Main Factory|Factory 1-3 -] - Duty Rate (MX)" or if blank, Retrieve from NT Orbit Duty Tools
Tariff Rate            - Master Chart "[Main Factory|Factory 1-3 -] - Tariff rate" or if blank, Retrieve from NT Orbit Duty Tools
Target FOB             - Line Plan
    - a sample table with desired fields is at lft.beproduct.Costing_Chart
    - at each refresh, fully overwrite this costing_chart using eligible fresh data
    - "NT Orbit Duty Tools" is calling a 3rd party API with some optimization in caching results. Intended in phase 9b.
    - Once (9b) finished, if there are changes in Costing chart HTS code or any of the 3 Duty rates or Tariff rates, i.e. any row called the 3rd party API, push the value back to Master chart  corresponding field. 



# checkpoint: update doc and housekeep orphan tables
1. let's drop the diagram.png and svg. And worth create a databricks_costing_interested_fields.txt similar to the 2 beproduct txts. PHASE5 worthy a dedicated WORKFLOW md? 
PHASE7 is enriching phase 1 like phase 3 really. May follow suite to create a dedicated doc too.
leaving out PHASE8_WORKFLOW and PHASE9_WORKFLOW for now until all planned subphases are completed.
2. reference live databricks lft.beproduct schema. there are dtc_fabric_ktb (the FABRIC sheets data), dtc_fabric_registry (the FABRIC sheet index), dtc_lineplan_registry, dtc_master_chart_uat, dtc_master_chart_uat_change_log, dtc_request_mapping, dtc_request_registry
- is there suppose to be a dtc_lineplan_ktb?
- what are the dtc_master_chart_uat* for? 
- should there be dtc_master_chart* in addition to above then?
- what is dtc_request_mapping?
3. for all the databricks notebooks, prefix them with corresponding phase number as far as possible. 
4. on MasterChart (WIP doc) - when create new rows / new sheet, put string 'Supplier' in column 'Supplier'

# 2026/08/14 test setup
During upcoming test cycle, BeProduct style drops into "TEST KTB" folder instead of normal "KTB" folder. And previous DTC requests will likely cleared (pending DTC team action).
Temporarily change the default Databricks job parameters to sync again "TEST KTB".

# 2026/08/28 check point
- DTC request - should register only 'active' Requests
    - previously registered requests could turn 'inactive' = hidden from user, this is same as 'delete'
    - Before data transformation, ensure requests in registry are active
    - Create new request if previously created target request become inactive
- For all new created request, share "" them to "aiagentwip@lifung.com" and user group "Kontoor Project Team"
- Requests can have the same name, and concurrently active, identified only by ID
- For the purpose of integration tracking, assume active WIP requests have unique names. Flag error otherwise.



# Phase 9B the NT Orbit Duty Tools for filling in missing HTS Code / Duty Rates / Tariff rates. $135.7
1. How to call NT Tools - it is an API.
- API Doc: https://orbitduty.neotangent.com/API-DOCS/#
- Entra OAuth2
    - Refresh token
    - use Personal account "auchunkei@lifung.com"
- Make costing chart table name a parameter. 
    - Default to "lft.beproduct.costing_chart"
    - Testing use "lft.beproduct.costing_chart_kei"

1.1 Authentication (Microsoft Entra ID / Azure AD)
The API requires a Microsoft Entra ID bearer token in the `Authorization` header. This is a delegated, per-user token obtained via the OAuth2 authorization-code + refresh-token flow.
- One-time setup (first use only)
    - Initiate Entra OAuth2 sequence.
    e.g. https://login.microsoftonline.com/%7B%7BTenantID%7D%7D/oauth2/v2.0/authorize?client_id=%7B%7BClientID%7D%7D&response_type=code&redirect_uri=XXXX&scope=https://graph.microsoft.com/.default%20offline_access&state=123456`
    create the callback for interactive auth if necessary, other use any well known auth flow alternatives.
    scope = https://graph.microsoft.com/.default

    - Stores the refresh token.
    - Refreshing the access token whenever necessary: Use refresh token to get new access token (~ every hour)
- !Take `access_token` from the JSON response → use as the Bearer token for Orbit API calls.!
- need to grant right for the user that call the Orbit API -> checks below endpoint to see if call success:
    https://orbitduty.neotangent.com/api/v1/health
    OK return reads like '{"status": "healthy", "timestamp": "2026-08-13T06:48:22.682Z", "version": "string"}'
 
1.2 Request Payload (From databricks lft.beproduct.costing_chart)
API = "/calculate/single/"
product_description:  Style Description (C), Content (I), Gender (J), Class (K), Sub Class (L) = Concatenate Columns C + I + J + K + L 
origin_country_code : Production Country  (Column P)
export_country_code : Production Country  (Column P)
import_country_code : Columns T / U / V headers | Each header encodes a market in brackets: T = *Duty Rate (US)* → `US`, U = *Duty Rate (CA)* → `CA`, V = *Duty Rate (MX)* → `MX`. Use the code corresponding to the column that applies. one API call per populated duty-rate column (i.e. up to 3 calls per row: US, CA, MX) |
de_minimis : false
mode_of_transport : "freight"
 
- General Duty — the base/standard duty line, always present in `detailed_lines`.
- Tariff — additional named duty lines beyond General Duty (e.g. *"10% Section 122 tariff on foreign-origin items"*, *"7.5% Section 301 tariff on goods originating from China"*). A single response can contain zero, one, or multiple tariff lines depending on origin/import country.
 
Example input 1:
{
  "product_description": "Cotton t-shirt with printed design",
  "origin_country_code": "BD",
  "import_country_code": "US",
  "export_country_code": "BD",
  "de_minimis": false,
  "mode_of_transport": "freight"
}
 
Example output 1:
{
  "success": true,
  "data": {
    "total_duty": 266.25,
    "duty_rate": 0.26625,
    "currency": "USD",
    "hs_code": "6109100012",
    "classification_name": "T-Shirts - cotton (knitted)",
    "detailed_lines": [
      {
        "name": "General Duty",
        "code": "6109100012",
        "amount": 165,
        "rate": 0.165,
        "type": "duty"
      },
      {
        "name": "10% Section 122 tariff on foreign-origin items",
        "code": "",
        "amount": 100,
        "rate": 0.1,
        "type": "duty"
      },
      {
        "name": "0.125% Harbor Maintenance Fee (HMF)",
        "code": "",
        "amount": 1.25,
        "rate": 0.00125,
        "type": "fee"
      }
    ],
    "product_classification_id": "2567",
    "dutify_id": "3601d989-a623-4747-87a7-a6e7128d4625",
    "created_at": "2026-06-30T08:50:57.153Z",
    "origin_country_code": "BD",
    "import_country_code": "US",
    "calculation_log": [
      "Processing API Response for BD -> US",
      "Shipment Value: $1,000.00 (unit $10 × 100)",
      "Identified HS Code: 6109100012",
      "Classification: T-Shirts - cotton (knitted)",
      "Parsed General Duty Rate from notes: 16.50%",
      "Found General Duty: $165.00 (16.50% of $1000)",
      "Found MPF: Merchandise Processing Fee (MPF) - Informal Entry by Sea (shown as note)",
      "Found duty: 10% Section 122 tariff on foreign-origin items - $100.00 (parsed rate: 10.00%)",
      "Found fee: 0.125% Harbor Maintenance Fee (HMF) - $1.25 (parsed rate: 0.13%)"
    ],
    "unit_price": 10,
    "has_mpf": true,
    "quantity": 100,
    "mode_of_transport": "freight",
    "total_shipment_value": 1000,
    "from_cache": false,
    "ai_classification": {
      "hs_code": "",
      "classification_id": "2567",
      "classification_name": "T-Shirts - cotton (knitted)",
      "confidence": "High",
      "reasoning": "The product 'Cotton t-shirt with printed design' was classified by first selecting 'Apparel & Accessories' (1820). Since no gender was specified, the 'Clothing for Men' (2344) branch was chosen as it contains standard garment definitions for adult apparel. Within that branch, 'T-Shirts and Tanks' (2566) was identified as the correct sub-group, and 'T-Shirts - cotton (knitted)' (2567) was selected as the specific leaf node, as t-shirts are fundamentally knitted cotton garments regardless of the printed design."
    }
  },
  "request_id": "1782809459482-9oufuuqyw",
  "processed_at": "2026-06-30T08:50:59.482Z"
}

1.3 Fields to fill (summary)
- duty_rate_xx (xx corresponds to import_country_code)
- tariff_rate (only present in "Found duty:" row when import_country_code='US')
- hts_code (= data.hs_code)

1.4 Push update to DTC
- transpose again like phase 9A, i.e. reverse  4 rows [Vendor, Factory] into single DTC row [(Main Vendor, Main Factory), (Vendor 1, Factory 1), (Vendor 2, Factory 2), (Vendor 3, Factory 3)]

2. Test the real oauth flow and actual api behaviour using env var

3. i am told to use my own entry id (auchunkei@lifung.com) instead of a separate app client ID for this. does it simplify / change above doc?
export NT_ORBIT_TENANT_ID=c4d8a220-a9ec-4572-8c77-ab36a3ecdbae
export NT_ORBIT_CLIENT_ID=c486611e-9bfc-49d5-8930-7d1943884b03
export NT_ORBIT_CLIENT_SECRET=XXXX

4.1 each api call is up to 30s. So the costing-chart update will take long - and should be grouped to call only when necessary - same input same output, if value already exist then no need to call again. 
Design a persistent caching mechanism, e.g. cache API results in table with a configurable TTL.

4.2 Make sure the OrbitDuty refresh token is self healing in databricks job runs


## checkpoint 2026/09/01
Confirm from project team Phase 8a (upload DTC Fabric sheets to databricks) and 8b are replaced by separate "MaterialLib" application. Can take out / disable from DAG.
update diagram.md to reflect current repo.
- which tables under lft.beproduct can be dropped? (dropped ktb_style_push_log, dtc_fabric_ktb, dtc_fabric_registry)
- project team will maintain multiple lineplan DTC requestion without specific naming convention for now (2026/9/1). Human in the loop to ensure each lineplan ref# is unique amonng all lineplan requests.
- lft.beproduct.costing_chart, found that factory-slot duplicates functions of supplier_type - use supplier_type and remove factory_slot.
    factory_slot/supplier_type: for the purpose of transpose between WIP & Costing chart, (Main Supplier, Main Factory), (Supplier 1, Factory 1), (Supplier 2, Factory 2), (Supplier 3, Factory 3) are 4 pairs of values each WIP row contains - total 8 fields. These split in to 4 rows in costing_chart, each row 2 fields. 
So we should only need 1 flag - i.e. Supplier_type to indicate whether it is (Main | 1| 2|3) for both supplier and factory.
- when run first time only main factory assigned, factory 2/3 not assigned; 2nd time factory 2/3 also assigned, will 2nd run duplicate main factory records or upsert costing_chart?
i will truncate costing_chart now so all get fresh populated.

- debug Lineplan: all rows in costing_chart should have lineplan_ref populated. It should be a WIP inner join Lineplan rather than left join.
- there are 8 rows in WIP known to have lineplan ref#, and there are also 8 corresponding rows in dtc_lineplan. So costing chart should contains exactly 8 rows at this moment.

# Phase 10 reading BOM $56
This is fulfilling phase 1 left over: BOM data is missing from BeProduct API and rely on Techpack extraction.
- BOM data is externally processed and stores at 
    - alb_tpm_uat.public.customer_teckpack_style_log (UAT)
    - alb_tpm_prd.public.customer_teckpack_style_log (PRD)
- Each BeProduct Style join to BOM table by BP.bp_style_no = BOM.style_no & (BP.Season || " - " || BP.year) = BOM.style_season (inner join only)
    - BOM.style_season in the format of "Spring - 2028"
- At each DAG job run, if a BP style finds corresponding BOM records, extract inline JSON BOM i.e. customer_teckpack_style_log.bom_unified:
    - validate json contains $.details.bom_detail_name["Main Fabric"] and/or $.details.bom_detail_name["Fabric"]. Only interested in these 2 segment types.
        - there should only be 1 "Main Fabric", and 0 or more "Fabric" segments.
    - If that style is already in DTC but without BOM, i.e. col_Fabric_Group = "MAIN MATERIAL CONTENT", 
         - assign dtc_wip_ktb.col_Fabric_Group =  $bom_detail_name
         - assign dtc_wip_ktb.col_Placement = $placement
         - assign dtc_wip_ktb.col_Mill_Fabric_Article# = $material_no
         - duplicate dtc row to a new row if more than one "Main Fabric" and "Body" exists, and populate above 3 fields with BOM data.
    - If that style already have one or more rows in dtc_wip_ktb with col_Fabric_Group <> "MAIN MATERIAL CONTENT", skip to next style. (No-ops)
- After BOM enrichment, there maybe new rows insert to DTC request.
- This DTC enrichment should happen before costing_chart construction, so to pipe up-to-date material names into OrbitDuty call.
- sample BOM JSON: [{"part": "BOM", "details": [{"bom_detail_name": "Main Fabric", "material_type": "Poplin", "material_name": "123455 - 97%Cotton 3%Spandex", "material_no": "WV-0064", "material_sub_type": "", "status": "", "composition": [{"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}, {"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}], "mill_supplier": "BEPRODUCT VENDOR", "component": "", "usage": "bodice", "placement": "bodice", "qty": null, "size": null, "colorway": []}, {"bom_detail_name": "Stitch/Seam", "material_type": "Polyester", "material_name": "TOPS THREAD T105", "material_no": "TH-000060", "material_sub_type": "", "status": "", "composition": [{"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}], "mill_supplier": "", "component": "", "usage": "bodice", "placement": "bodice", "qty": null, "size": null, "colorway": []}, {"bom_detail_name": "Trim", "material_type": "Button", "material_name": "16L PLASTIC BUTTON DTM", "material_no": "16L PLASTIC BUTTON DTM", "material_sub_type": "", "status": "", "composition": [{"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}], "mill_supplier": "BEPRODUCT VENDOR", "component": "", "usage": "", "placement": "", "qty": null, "size": null, "colorway": []}, {"bom_detail_name": "Trim", "material_type": "Drawstring", "material_name": "Drawstring", "material_no": "DS-000075", "material_sub_type": "", "status": "", "composition": [{"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}], "mill_supplier": "", "component": "", "usage": "Center front", "placement": "Center front", "qty": "1.000", "size": null, "colorway": []}, {"bom_detail_name": "Label", "material_type": "Main Label", "material_name": "WOMEN Label with folded edges", "material_no": "25-3380-01", "material_sub_type": "", "status": "", "composition": [{"fiber_pecentage": "", "fiber_content": "", "fiber_coo": "", "yarn_type": "", "yarn_coo": "", "trademark_certificate": ""}], "mill_supplier": "ZHEJIANG CHARMING DYEING AND FINISHING CO.,LTD.", "component": "", "usage": "", "placement": "", "qty": null, "size": null, "colorway": []}], "column_header": ["**BomHeader", "**MaterialCategory", "**MaterialCode", "**MaterialType", "**MaterialDescription", "**Quantity", "**MaterialContent", "**MaterialConstruction", "**MaterialCuttableWidth", "**Placement", "**Size", "**RefNo", "**Section", "**InternalName", "**SupplierName", "**SupplierCop", "**SupplierRefNo", "**MainMaterial", "**UOM", "**CountryOfOrigin", "**CostFob", "**CostCif", "**WeightBeforeWash", "**WeightAfterWash", "**WeightUOM", "**Comments", "COMPONENT", "MATERIAL/FABRIC WEIGHT"]}]         

## checking DAG
question is fill_bom_data, logically it should read from all BeProduct styles changes at the start of run, so it should see the image after original phase 1 complete: style x color (phase 3 image is optional). Then phase 10 logic read BOM to complete the style x color x material WIP master chart. Then phase 9 logic based on this to create costing chart.
phase 3 while not a blocker, expects to fill all rows (style x color x material) with images, so effectively it should be after phase 10 but can be parallel to phase 9. Just that both phase 9 and phase 3 may be updating same set of rows, and given user may have edited the row in DTC, so each of phase 3 and phase 9 should be repulling the request before edit.

## validating DAG (3/9/26)
- validating customer_teckpack_style_log: 
    - use table "customer_teckpack_style_latest", each style_no + customer_name + style_season returns at most 1 row 
    - change of BOM_UNIFIED: base on Fabric_Group + Mill_Fabric_Article#, *upsert* only, if BOM disappeared or material removed, keep the DTC row untouch.
    - basically the only potential field to be update in this area is "placement"
    - note: "customer_department" is part of "customer_teckpack_style_latest" unique key. In Kontoor, this is always "brand".
- rethinking sequence of whole job
00. DTC -> BeProduct - factory / supplier masters (phase 0)
10. BeProduct -> DTC - DTC contains rows style x color (phase 1 + 4 + 7)
20. DTC -> BeProduct - BeProduct style x color take update from DTC (phase 2)
30. TPM -> DTC  style x color x BOM (phase 10)
40. DTC -> Costing_Chart style x BOM x LinePlan (phase 9a)
50. Costing chart get enriched by OrbitDuty API (phase 9b)
55. DTC rows get duty / tariff rates (phase 9b)
60. DTC rows get images  (phase 3)

- Known limitation : DTC block a browser user to 'save' if the on screen request bears an older "last_read" timestamp than the one server hosting.
    - i.e. If multiple users open the same DTC request and make changes, the first who click 'save' will preserve, others will have their 'save' action rejected. They have to refresh the whole request to retry.
    - the same if API updated / insert a row in a request while user of interactive session are making changes on the same request, user will lost changes they made silently.
    - this is being addressed by DTC team to make "concurrent edit" 
- would this make more sense:
    - "DTC rows get duty / tariff rate" run as separate jobs. It does not read / rely on DTC request directly. It reads and update databricks costing_chart table only.
        - likely split phase 9b into 2 tasks (50 & 55)
    - "DTC rows get images" can also run separately, fill in missing images against DTC direcly (60).
    - The main job handles 00-10-20-30-40-55 
- use Standard_D4as_v5 as job cluster node type

## costing chart (phase 9) & DTC product image (phase 3) clarifications
- while populating costing chart from WIP x LinePlan requests, only take WIP rows where fabric details ("Content", "Fabric Type") are not null.
- these fields are populated by DTC internal trigger that poll "Mill Fabric Article #"
//- at step 55, check if current DTC request row has a newer modified date than corresponding orbit API cache result. 
//    - if not, fill the newer Duty result to DTC. 
//    - if so, the DTC row should contain up-to-date already
- update Tariff/Duty/HS_Code every time
- when phase 3 runs, scan rows without "Style image" and first look up whether other rows of same BP_STYLE_NO on the same request (i.e. same Brand same season) already have an image.
    - if so, copy the image locator
    - if not, do the full image extraction from BP flow
- when ever issuing DTC PATCH API against WIP request, according to interested fields mapping, the body should include only all or some of these fields of WIP document:
    - Product_status
    - Style_Image
    - LF_style_no
    - Legacy_code
    - BP_Style_no
    - Style_description
    - class
    - sub_class
    - Division
    - brand
    - color_wash
    - Garment_finish
    - Tech_pack_stage
    - Proto_Sample_Status
    - Pre-Line sample status
    - SMS Sample Status
    - 2nd fit sample status
    - Top sample approval status
    - PP sample submission approval status
    - Fabric_Group
    - Placement
    - Mill_Fabric_Article
  do not include other fields in PATCH body.

## Costing_chart clarifications
- since multiple "material_no" can have same "content", and multiple style could share a lineplan, the key for costing chart should be "bp_style_no" + "lineplan_ref" + "material_no"
- "material_no" is not in costing chart at this moment. add to table.
- the check should be against "bp_style_no" + "lineplan_ref" + "material_no"
- dtc.content: while it will be populated by DTC's own trigger, for our purpose we can bring BOM.material_name to populate it. Then phase9a will not be blocked.

- take out 'brands' from BP tables - use 'brand' (brand_hk) only now
- BeProduct "Customer Factory Code" should be populated with DTC "Main Factory Customer ID"
- update docs

